namespace CouchDB.Driver.Types
{
    /// <summary>
    /// Represent the direction of the index.
    /// </summary>
    public enum IndexFieldDirection
    {
        Ascending = 0,
        Descending = 1,
    }
}